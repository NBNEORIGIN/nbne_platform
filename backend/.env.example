# Database Configuration
DB_NAME=booking_db
DB_USER=postgres
DB_PASSWORD=change_me_in_production
DB_HOST=localhost
DB_PORT=5432
# Or use DATABASE_URL for Railway:
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Django Settings
SECRET_KEY=django-insecure-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Module Feature Flags (all default to True)
BOOKINGS_MODULE_ENABLED=True
PAYMENTS_MODULE_ENABLED=True
STAFF_MODULE_ENABLED=True
COMMS_MODULE_ENABLED=True
COMPLIANCE_MODULE_ENABLED=True
DOCUMENTS_MODULE_ENABLED=True
CRM_MODULE_ENABLED=True
ANALYTICS_MODULE_ENABLED=True
TENANTS_MODULE_ENABLED=True

# Email (development — console backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Email (production — SMTP)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.ionos.co.uk
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your@email.com
# EMAIL_HOST_PASSWORD=your_password
# DEFAULT_FROM_EMAIL=your@email.com

# Booking Reminder Email (separate SMTP credentials)
# REMINDER_EMAIL_HOST=smtp.ionos.co.uk
# REMINDER_EMAIL_PORT=465
# REMINDER_EMAIL_USE_SSL=True
# REMINDER_EMAIL_HOST_USER=reminders@yourdomain.com
# REMINDER_EMAIL_HOST_PASSWORD=your_password
# REMINDER_FROM_EMAIL=reminders@yourdomain.com

# Stripe Payments
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Resend API (fallback email provider)
# RESEND_API_KEY=re_...

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000
